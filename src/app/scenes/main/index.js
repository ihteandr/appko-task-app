import { connect } from 'react-redux';
import MainView from './view';

function mapStateToProps(state) {
    return {};
}

function bindActionsToProps() {
    return {};
}

export default connect(mapStateToProps, bindActionsToProps)(MainView);